package com.w2sv.navigator.domain.moving

import com.anggrayudi.storage.media.MediaType
import com.w2sv.common.uri.MediaId

data class MediaIdWithMediaType(val mediaId: MediaId, val mediaType: MediaType)
